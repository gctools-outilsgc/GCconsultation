<% provide :title do %>
  <%= t("test.header.title") %> - <%= t("test.menu.legislation") %> - <%= t("test.legislation.processes.index.filters.#{@current_filter}") %>
<% end %>

<h2 class="inline-block"><%= t("test.legislation.processes.index.title") %></h2>

<%= link_to t("test.legislation.processes.index.create"), new_test_legislation_process_path,
            class: "button float-right" %>

<%= render 'shared/filter_subnav', i18n_namespace: "test.legislation.processes.index" %>

<% if @processes.any? %>
  <h3><%= page_entries_info @processes %></h3>

  <table class="stack">
    <thead>
      <tr>
        <th><%= t("test.legislation.processes.process.title") %></th>
        <th><%= t("test.legislation.processes.process.status") %></th>
        <th><%= t("test.legislation.processes.process.creation_date") %></th>
        <th class="text-center"><%= t("test.legislation.processes.process.comments") %></th>
        <th><%= t("test.actions.actions") %></th>
      </tr>
    </thead>

    <tbody>
      <% @processes.each do |process| %>
        <tr id="<%= dom_id(process) %>">
          <td class="small-12 medium-6">
            <%= link_to process.title, edit_test_legislation_process_path(process) %>
          </td>
          <td><%= t("test.legislation.processes.process.status_#{process.status}") %></td>
          <td><%= I18n.l process.created_at.to_date %></td>
          <td class="text-center"><%= process.total_comments %></td>
          <td>
            <%= link_to t("test.legislation.processes.index.delete"), test_legislation_process_path(process),
                        method: :delete,
                        class: 'button hollow alert expanded' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= paginate @processes %>
<% else %>
  <div class="callout primary">
    <%= page_entries_info @processes %>
  </div>
<% end %>
