<%= render "test/shared/globalize_tabs",
           resource: @process,
           display_style: lambda { |locale| enable_translation_style(@process, locale) } %>

<%= translatable_form_for [:test, @process], url: url, html: {data: {watch_changes: true}} do |f| %>

  <%= render "shared/errors", resource: @process %>

  <div class="small-12 column margin-top">
    <%= f.check_box :homepage_enabled, label: t("test.legislation.processes.form.homepage_enabled") %>
  </div>

  <%= f.translatable_fields do |translations_form| %>
    <div class="small-12 column">
      <div class="ckeditor">
        <%= translations_form.cktext_area :homepage,
                                          language: I18n.locale,
                                          label: t("test.legislation.processes.form.homepage"),
                                          ckeditor: { height: 500, toolbar: 'test' },
                                          hint: t("test.legislation.processes.form.homepage_description") %>
      </div>
    </div>
  <% end %>

  <div class="small-12 medium-3 column end margin-top">
    <%= f.submit(class: "button success expanded", value: t("test.legislation.processes.#{test_submit_action(@process)}.submit_button")) %>
  </div>
<% end %>
